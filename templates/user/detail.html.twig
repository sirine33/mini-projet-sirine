{% extends 'base.html.twig' %}

{% block title %}List Produit{% endblock %}

{% block body %}
<h1>liste des produits</h1>
<table class="table">
  <thead>
    <tr>
      <th scope="col">User</th>
      <th scope="col">Email</th>
    </tr>
  </thead>
  <tbody>
  
    <tr>
      
      <td scope="row">{{products.Lastname}}</td>
      <td>{{products.email}} </td>
      
    </tr>

  </tbody>
  
</table>
<a href="{{path('update',{'id':products.id})}}" class="btn btn-success">Mettre A Jour</a>
<a href="{{path('delete1',{'id':products.id})}}" class="btn btn-danger">Supprimer</a>


{% endblock %}
